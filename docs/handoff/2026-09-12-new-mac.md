# New Mac verification — September 12, 2026

Local setup and verification are complete. The sections below record the setup snapshot; development later resumed and photo PR4 merged. See [the photo release record](../photos/release-2026-09-12.md) for the newer deployment state and the [routine plan](../superpowers/plans/2026-09-12-routine-loop.md) for the active next goal. Hosted clinical and device release gates remain open.

## Repository and preservation

Cloned GitHub's `main` into `~/code/ClearAF`, at `84a83662848942d4f45d1fa12c1fe7a77314fe3b`. The destination initially contained only `.DS_Store`, which was preserved. No private backup had been transferred during setup. A private transfer arrived later the same day and was inspected separately; it was not applied to the checkout. No production patient data was used.

PRs #1, #2 and #3 are merged. The handoff's instructions to start from the stacked T2 branch and its statements that T1 remains open are historical. New development starts from current `main`.

The [main verification run](https://github.com/Arluigi/ClearAF/actions/runs/34645382980) passed backend, portal, database-recovery and secrets jobs. CI does not run Simulator tests.

## Development environment

- Apple Silicon, macOS 26.6; Xcode 26.6 (17F113), iOS 26.5 Simulator (23F77), iPhone 17.
- Node 24.4.0 and npm 11.4.2, using the official checksum-verified arm64 archive under `~/.local/share/clearaf`.
- Pinned Gitleaks 8.30.1 is installed in `~/.local/share/clearaf/bin`.
- Homebrew; Colima 0.10.3, Docker CLI 29.8.0, libpq 18.6, GitHub CLI 2.100.0 and Vercel CLI 59.16.0.
- Dedicated `clearaf` Colima profile: 2 CPUs, 3 GB memory, 8 GB data disk and 8 GB root disk. The scripts select its Docker socket explicitly.
- Pinned Supabase CLI 2.117.0 and canonical migration `20260910183704`; local migration history matches.
- GitHub CLI and Vercel sign-in verified. Git uses the user's authenticated GitHub credential helper. This repository uses the existing public GitHub author identity.
- `~/.zprofile` adds the development tools to PATH and selects the full Xcode toolchain through `DEVELOPER_DIR`. Open a new login shell to pick these up.
- Superpowers 6.3.0 installed through Codex's marketplace; `gh-fix-ci` and `gh-address-comments` installed from OpenAI's curated skills. The GitHub connector was already available.

The existing planning convention is `docs/superpowers/plans`, with bounded implementation and independent review. Repository evidence also includes GitGuardian configuration, pinned Gitleaks scanning, shadcn/ui, and the Supabase/Vercel workflow. These do not establish which other plugins were installed on the old Mac.

## Fresh local checks

| Check | Result |
| --- | --- |
| Backend clean dependency install, tests, build | Passed; 62 tests |
| Portal clean dependency install, tests, build, typecheck | Passed; 23 tests |
| Portal lint | 0 errors, 45 existing warnings |
| Backend and portal dependency audits | 0 vulnerabilities each |
| Source hygiene | Passed |
| Pinned Gitleaks 8.30.1 scan after the audited T0 baseline | No leaks found |
| Local API readiness | Ready |
| Local account suite | 6 groups passed; fixtures cleaned |
| Fresh-destination recovery | Passed in 37 seconds; database/Auth/storage bytes restored and 27 authorization checks passed |
| Portal browser | Clinician login, reload restoration, logout, patient denial, real local PKCE callback and recovery cancellation verified |
| iOS Simulator Debug | Build passed; 19 unit tests and both account UI tests passed |
| iOS Simulator Release | Build passed; not launched against production |

The first sandboxed backend test attempt could not bind loopback servers, and the first portal build could not download Google Fonts. Both passed after granting the required execution/network permissions. No application code was changed to bypass these checks.

The first Simulator run passed account-switch/recovery UI coverage but failed the onboarding flow because Apple's Save Password prompt covered the Next button. Its captured accessibility hierarchy used a remote view rather than a sheet. The test helper now identifies the prompt by its title and dismisses its Not Now button, without changing application behavior. A second issue in the test tapped the full-name field during the animated switch to signup and left the email field focused. Both registration paths now wait for the full-name field to exist before tapping it. These test-only changes received independent review. The final complete run passed with zero failures (`/private/tmp/clearaf-account-synchronized.xcresult`); Release Simulator build also passed. Earlier failed attempts remain in ignored local logs for traceability. No application behavior was changed.

Apple registered two copies of the same Simulator runtime. The unusable duplicate was removed by exact ID using `simctl runtime delete --keep-asset`, after a dry run; the ready image was retained. The duplicate removal left the retained image's mount registration stale. Unmounting and rescanning restored it, and the iPhone 17 booted successfully. The final runtime inventory contains one ready 7.9 GB image. The available evidence does not establish what initiated the duplicate download.

The recovery drill completed before browser fixtures were created. Browser fixtures were subsequently removed with the documented identity-scoped cleanup. All eight UI test identities created during this setup were then removed by exact, revalidated ID/email pairs against the verified loopback database and Auth service. The UI ended signed out. Generated configuration is ignored and has mode `0600`; no dotenv contents belong in logs or commits. Detailed local evidence is in ignored `.local/new-mac-verification.json` and `.local/recovery-verification.json`.

## Verified deployment state

Authorized Vercel team: `arluigis-projects`.

| Component | Immutable deployment | Source | Status |
| --- | --- | --- | --- |
| API | `dpl_FLsoF91Fs8SAvXCLG2XJooto4ESr` / `clearaf-gv8qsqx2g-arluigis-projects.vercel.app` | `c3de071f3c60e252042ae0ee1388bbdc5d5b5f1f`, T1 | Ready; public health and readiness passed |
| Portal | `dpl_D5uBxTy8nXWLLwLkLGfaGPm34WSQ` / `clearaf-portal-jqqo371bp-arluigis-projects.vercel.app` | `84a83662848942d4f45d1fa12c1fe7a77314fe3b`, merged main | Ready; HTTP 200 |

The portal is Git-connected to `main` with root `web-portal`. The API is a separate manually deployed project with no Git link. **The API has not received T2.** No production deployment or database mutation was performed during setup. Git metadata on a manual deployment identifies its recorded source; it does not independently prove the uploader's worktree was clean.

Hosted email confirmation/redirect configuration, an authorized hosted account smoke test, clinical retention/removal policy, and physical-device/distribution signing remain release gates. Local success does not satisfy them.

## What is missing after T2

GitHub contains T1/T2 plans but no post-T2 roadmap. After setup, the user transferred a private backup containing the missing proposed roadmap and audit. The backup remains excluded from Git and deployment uploads; it was not restored over main. `CLAUDE.md` is historical: public photo hosting, automatic assignment, and retired WebSocket ideas must not override the current security boundary.

Current code still contains simulated iOS clinician replies and local-only message/photo delivery claims in `ClearAF/Views/MessagingView.swift`, an unimplemented `fetchMessages`, placeholder portal conversation metadata and static notification counts, and unfinished appointment/profile actions. These are concrete unfinished clinical workflow integration work, not evidence of an approved T3 scope.

The recovered roadmap identifies T3 as the photo/routine core loop, T4 as experience simplification/testing, and T5 as pilot/release. It proposes deferring messaging and prescribing. This supersedes the preliminary suggestion to implement communication made before the roadmap arrived. Remove fabricated replies and delivery claims without treating messaging as approved MVP scope. Clinical retention, account removal, assignment policy, response expectations, and treatment/score interpretation must come from product/clinical guidance; they must not be inferred from demo code. At setup completion, application development was on hold. The user subsequently resumed development, approved MVP scope and clinician-owned routines, and requested goal-based implementation. Photo PR4 is now merged; the routine goal is active.

## Ready to use

The local backend is running at `http://127.0.0.1:3001`, the portal at `http://localhost:3000`, and local mail capture at `http://127.0.0.1:54324`. Use the Debug scheme in `ClearAF.xcodeproj` with the iPhone 17 Simulator. For restart commands, follow [the local baseline](../baseline/README.md#local-setup).

The setup originally left two handoff documents and a small UI-test compatibility adjustment uncommitted. The compatibility adjustment was included in photo PR4. Original handoff notes were preserved when main was updated, and this document retains their historical setup evidence. Subsequent feature commits and deployments are recorded separately.
