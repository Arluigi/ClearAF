# Photo foundation merge and deployment — September 12, 2026

The user authorized commit, push and merge after local verification, then requested the next routine goal. [PR4](https://github.com/Arluigi/ClearAF/pull/4) merged at `2a7f3925552c62d0194630d0d439fd46cf307c88`. Both branch/PR check sets passed, including real database recovery, backend/portal and secret checks. [Post-merge CI](https://github.com/Arluigi/ClearAF/actions/runs/34719296911) also passed. Local evidence remains in [verification.json](verification.json); its no-push/no-deployment entry describes the pre-authorization snapshot.

The Git-connected portal deployed automatically from the exact merge commit. The manually deployed API was built from a clean `git archive` of that same commit's backend directory, linked to the existing `clearaf-api` project and deployed using the documented CLI production procedure. No local dotenv, private backup or untracked checkout content was included, and no production database migration was run.

| Component | Immutable deployment | Source | Verification |
|---|---|---|---|
| Portal | `clearaf-portal-5o50mlycu-arluigis-projects.vercel.app` | `2a7f392` | Vercel READY, production alias HTTP200 |
| API | `dpl_HRDf2TpEStpHMKd17vESCfRLQRZJ` / `clearaf-cuknah4oc-arluigis-projects.vercel.app` | `2a7f392` recorded and clean archive uploaded | Vercel READY, `/health` healthy, `/ready` ready, unauthenticated capture request401 |

API project runtime remains Node24.x. The prior secure rollback target was saved before deployment: `dpl_FLsoF91Fs8SAvXCLG2XJooto4ESr` / `clearaf-gv8qsqx2g-arluigis-projects.vercel.app` (T1 `c3de071f`). No schema change is involved in the photo release. If rollback is needed, follow the baseline procedure and recheck security/readiness; code rollback is not database recovery.

The old API/T2 deployment gap is closed. No production patient data or authenticated clinical workflow was used for smoke testing. Hosted clinical acceptance, clinical retention/removal and physical-device/distribution checks remain separate release gates. The next active development goal is clinician assignment/editing of morning/evening routines and reliable patient completion, documented in the [routine plan](../superpowers/plans/2026-09-12-routine-loop.md).
